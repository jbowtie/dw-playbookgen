<h2>New move</h2>

<%= render "form.html", changeset: @changeset,
                        action: playbook_move_path(@conn, :create, @playbook) %>

<%= link "Back", to: playbook_move_path(@conn, :index, @playbook) %>
