<header>
  <h1 class="ui block header">Edit campaign</h1>
<div class="ui large breadcrumb">
  <a class="section" href="/">Home</a>
  <div class="divider"> / </div>
  <div class="active section"><%= @campaign.name %></div>
</div>
</header>

<div class="ui grid">
  <div class="twelve wide column">
<%= render "form.html", changeset: @changeset,
action: campaign_path(@conn, :update, @campaign) %>
</div>

<div class="four wide column">
<ul class="ui fluid right vertical menu" style="padding-left:0;">
  <li class=" active item"><h3 class="header">Playbooks</h3></li>
  <%= for p <- @campaign.playbooks do %>
    <li class="link item"><%= link p.name, to: playbook_path(@conn, :edit, p) %></li>
  <%end%>
  <li class="item"><%= link "Add playbook", to: playbook_path(@conn, :new), class: "ui button" %></li>

  <li class=" active item"><h3 class="header">Basic Moves</h3></li>
  <%= for m <- starting @campaign.moves do %>
    <li class="link item"><%= link m.title, to: campaign_campaign_move_path(@conn, :edit, @campaign, m) %></li>
  <%end%>
  <li class="item"><%= link "Add move", to: campaign_campaign_move_path(@conn, :new, @campaign, level: 1), class: "ui button" %></li>
  <li class=" active item"><h3 class="header">Special Moves</h3></li>
  <%= for m <- advanced @campaign.moves do %>
    <li class="link item"><%= link m.title, to: campaign_campaign_move_path(@conn, :edit, @campaign, m) %></li>
  <%end%>
  <li class="item"><%= link "Add move", to: campaign_campaign_move_path(@conn, :new, @campaign, level: 2), class: "ui button" %></li>
</ul>
</div>
</div>
