<h2>Campaign Playbooks</h2>
<p>Here are your choices.</p>

<%= for book <- @books do %>
<article>
  <h3><%= book.name %></h3>
  <%= markdown book.summary %>
  <p><%= link "Playbook", to: page_path(@conn, :show, book.slug) %></p>
</article>
<% end %>
